///init()
//All the stuff that should run by default

//Make me big pixls
global.scale = 4;

//What the camera should be looking at
global.focus = 0; //First object created (Probably the camera)

room_goto(rm_level_1);
//room_goto(Level_Editor);
